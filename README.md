# `@uipatterns/eslint-config`

[ESLint](https://eslint.org/) config for uipatterns.

## Usage

**Edit `package.json`**:

```jsonc
{
  // ...
  "eslintConfig": {
    "extends": "@uipatterns/eslint-config"
  }
}
```
